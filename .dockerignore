.dockerignore
# there are valid reasons to keep the .git, namely so that you can get the
# current commit hash
#.git
.log
tmp

# Mix artifacts
_build
deps
*.ez
releases

# Generate on crash by the VM
erl_crash.dump

# Secret config for prod or local dev-specific work not to be committed.
config/*.secret.exs

# Dialyzer
/priv/plts/*.plt
/priv/plts/*.plt.hash
