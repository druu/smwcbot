.dockerignore
# there are valid reasons to keep the .git, namely so that you can get the
# current commit hash
#.git
.log
tmp

# Mix artifacts
_build
deps
*.ez
releases

# Generate on crash by the VM
erl_crash.dump

# Static artifacts
node_modules

# Secret config for dev-specific work not to be committed.
config/dev.secret.exs

# Dialyzer
/priv/plts/*.plt
/priv/plts/*.plt.hash
